; It will cause an Aborting! Because maximum recursion depth exceeded.
; new-if is a procedure. When evaluate this procedure, the interpreter 
; whill evaluate all of its' arguments first. Thus, it will generate an 
; aborting that maximum recursion depth exceeded.
